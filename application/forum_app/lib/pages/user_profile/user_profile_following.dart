import 'package:flutter/material.dart';

class UserProfileFollowing extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Center(
        child: Text('Following'),
      ),
    );
  }
}