import 'package:flutter/material.dart';

class UserProfileHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Center(
        child: Text('History'),
      ),
    );
  }
}