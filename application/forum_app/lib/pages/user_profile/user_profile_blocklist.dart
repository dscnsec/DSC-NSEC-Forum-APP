import 'package:flutter/material.dart';

class UserProfileBlocklist extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Center(
        child: Text('Blocklist'),
      ),
    );
  }
}