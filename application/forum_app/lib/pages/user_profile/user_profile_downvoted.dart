import 'package:flutter/material.dart';

class UserProfileDownvoted extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Center(
        child: Text('Downvoted'),
      ),
    );
  }
}