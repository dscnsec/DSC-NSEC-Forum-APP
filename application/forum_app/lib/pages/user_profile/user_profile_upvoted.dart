import 'package:flutter/material.dart';

class UserProfileUpvoted extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Center(
        child: Text('Upvoted'),
      ),
    );
  }
}