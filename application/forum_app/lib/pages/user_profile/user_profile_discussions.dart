import 'package:flutter/material.dart';

class UserProfileDiscussions extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Center(
        child: Text('Discussions'),
      ),
    );
  }
}