import 'package:flutter/material.dart';

class UserProfileMentions extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Center(
        child: Text('Mentions'),
      ),
    );
  }
}